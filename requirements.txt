# Core dependencies
argparse   # Standard library, no install needed

# Optional: for better formatting
colorama>=0.4.6
rich>=13.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Type checking
mypy>=1.0.0
